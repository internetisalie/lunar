---
id: "ROADMAP"
title: "Project Roadmap"
type: "guide"
priority: "high"
folders:
  - "[[features]]"
---

# Project Roadmap

> **Durable value = the ordering and dependency edges, not the `Status` column.** Canonical
> per-feature status is each feature's `requirements.md` front-matter (`status:`); the `Status`
> here is advisory. Only **open** work is listed — completed waves (0–19) are done and
> live in git history.

> **MVP COMPLETE (2026-07-18).** The product is **feature-complete for MVP** — Wave 18 (SYNTAX-18,
> MAINT-23, TYPE-10, REDIS-06, ROCKS-16, TOOLING-08) and Wave 19 (codebase-review remediation,
> MAINT-24 through 32) both shipped and released (v0.19.0 / v0.19.1). Those two completed waves are
> removed from this roadmap and live in git history. **Remaining work is all post-MVP:** the
> **Wave 20 follow-ons** (corpus-sweep fixes + the consolidated loose backlog, incl. MAINT-21 —
> deferred, externally blocked on the unreleased 2026.2 platform), the **Wave 21 definition-library
> stream** (TARGET-07/08/09, MAINT-34 and their bug follow-ons), and the **AI epic (Wave 22)**.
>
> **Waves map to releases: wave N ships as `v0.N.x`.** Wave 20 → v0.20.0/v0.20.1,
> Wave 21 → v0.21.0.

## How an agent uses this

- **Pick the lowest-numbered wave with a *ready* item** — ready = every `Depends on` is `done`;
  within a wave prefer higher priority.
- **Parallel ✓** = new files / a distinct extension point → safe to run concurrently in separate
  worktrees. **Serial: <cluster>** = mutates a shared hot file → one agent at a time in that cluster.
- **Update `status` to `done`** in the feature's `requirements.md` as you finish; that makes its
  dependents ready.
- **DoD gate (learned the hard way):** a feature surfacing through a platform extension point
  (inspection, annotator, completion, refactoring, safe-delete) is "done" only when a **real-flow**
  test drives that machinery (`enableInspections()+doHighlighting()`, `completeBasic()`,
  `SafeDeleteHandler.invoke`, …) and asserts the user-visible result — engine-only tests hid a real
  REFACT-03 bug.

## ⚠️ Unmerged feature branches to recover (flagged 2026-07-06)

Front-matter reports **every epic done** (TOOLING completed 2026-07-09), but three git branches
carry **unmerged commits** for supposedly-complete features. They were **kept** (not deleted) during a
branch cleanup. Before trusting "all done," reconcile each against `main`/the front-matter
and either integrate it (verify with the real-flow DoD gate above) or consciously discard it:

| Branch (local; ✎ = also on gitea) | Tip | What it is | Action |
| :--- | :--- | :--- | :--- |
| `feature/COMP-03-02-global-symbol-suggestions` | `dac5fb83` | **Completed feature** — "Implement Global Symbol Suggestions" (not WIP; not on `main`) | Verify vs COMP-03-02 status; likely integrate |
| `feature/syntax-inlay-hints-method-chaining` ✎ | `105e87b3` | WIP — method-chaining inlay hints (relates to SYNTAX-07/-17) | Finish or discard |
| `wip/lua-types-visitor` | `c820bf63` | WIP — `LuaTypesVisitor`, rescued from a stash (type engine) | Finish or discard |

Each is 1 commit ahead of `main`. Recover a branch's work with `git cherry-pick <tip>` (or merge)
onto a fresh feature branch; the SHAs above are stable references even if a branch is later pruned.

---

## Wave 20 — Corpus sweep & consolidated backlog  *(non-gating; shipped as v0.20.x)*

> The **MAINT-33** corpus sweep and the four defects it surfaced (BUG-389/390/391/392, with BUG-393
> still open) — this is what shipped as **v0.20.0 / v0.20.1**.
>
> Also carries the consolidated loose backlog (folded in 2026-07-18) so the roadmap is the single
> source: the standalone **REDIS-07** feature, designed-but-unshipped UIs (**BUG-381 / BUG-388**),
> the two still-open older bugs (**BUG-358 / BUG-359**), and **MAINT-21** (deferred). These are not
> tied to the v0.20.x releases; they are parked here rather than in a wave of their own. Bugs also
> live under `docs/features/bug-fixes/`.
>
> **Note:** BUG-359 is closed by **BUG-397**, which now sits in Wave 21.

| ID | Title | Status | Prio | Depends on | Unblocks | Parallel |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| MAINT-21| IJPGP 2.17 + Gradle 9 bump (deferred from MAINT-03-04) | deferred | L | **2026.2 platform release** *(build 262; not yet shipped as of 2026-07-03)* | — | Spike proved Gradle 9.1 + IJPGP 2.17 build clean but 2.17's test framework needs a platform newer than 261; land with the SDK bump. Untried: intermediate IJPGP ~2.7–2.16 for a 261-compatible Gradle-9 upgrade |
| REDIS-07 | Reuse an IntelliJ Database Redis data source | planned | C | REDIS-01 *(connections, done)* | — | ✓ redis/ → `docs/features/redis/07-database-datasource-integration/`: reuse an IntelliJ Database-plugin Redis data source as a connection source (vs a hand-entered connection) |
| BUG-381 | Ephemeral Redis/Valkey provisioning **UI** | planned | M | provisioning capability *(built, done)* | — | ✓ Docker/local-binary provisioning is implemented end-to-end but has **no UI entry point** (unreachable without hand-editing XML); fully planned (design + impl-plan, 3 phases) → `docs/features/bug-fixes/381-.../` |
| BUG-388 | Dimmed-text binding-state renderer | backlog | C | BUG-387 *(done)* | — | ✓ UX polish: convey inherited/no-default via `GRAYED_ATTRIBUTES` in a `ColoredListCellRenderer` instead of a parenthetical → `docs/features/bug-fixes/388-.../` |
| BUG-358 / 359 | Two still-open older bugs | todo (358) | C | — | — | ✓ **BUG-358** reformatting a read-only file throws a TransactionGuard write-unsafe exception (still open); **BUG-359 closed 2026-08-05 by BUG-397 Phase 2**, as predicted — false-positive `nil value is not assignable to string` on `package.path` — **diagnosed 2026-08-04: not its own defect, it is BUG-397.** The RHS infers `String` correctly; the LHS `package.path` is `Undefined` because the type engine is deliberately file-local for free globals, so a `String` is assigned into a slot believed to be nothing. Fix BUG-397 and this closes with it; do not plan it separately. Reconciled 2026-07-18 — 354/355 fixed, 360 closed (env/harness), 356/363/365/366/367/368/369 fixed-via-features |
| MAINT-35 | Lexer/parser torture testing — differential oracle + invariants | todo | S | MAINT-33 *(done)* | — | ✓ **Drafted 2026-08-05** → `docs/features/maint/35-parser-torture-testing/`. MAINT-33 counts `parseErrors` but **cannot tell a true parse error from a false one** — BUG-392 (valid luarocks Lua rejected) sat in a baseline as an expected number until a human read the file. Adds the judge: a **version-matched `luac -p` oracle** comparing accept/reject against Lunar's `parseErrors == 0`, both directions. Version matching is load-bearing and verified — `luac5.4` accepts `1 // 2`, `luac5.1` rejects it. Plus two oracle-free invariants, each chosen because a shipped defect would have been caught: **lexer round-trip** (token texts must reconstitute the source byte-for-byte — catches *lost* text) plus **unmerged-token counting** (internal `LONGSTRING*` escaping the merge → BUG-392's actual signature; mutation proved the round-trip alone misses it, since that defect re-partitioned rather than lost characters) and **crash-freedom** catching `Throwable` (→ BUG-390's StackOverflowError). **MAINT-35-00 owns the dependency by pinning it**: `luac` is built from a sha256-pinned lua.org tarball into `test/luac/<version>/`, resolved by table lookup, `PATH` never consulted. Two earlier drafts were wrong in opposite directions — the first treated a missing `luac` as a normal runtime state and grew a nullable metric, a four-case comparison table and a CI skip rule to survive it; the second installed it from apt, which is *unpinned inside a ratchet* (Debian ships patched `5.1.5-11`/`5.4.7-1+b2`, floating with the distro) and would let two machines judge by different oracles while the gate looked uniform. Only `build-essential` now comes from the system — a compiler does not decide verdicts. Prompted by squeek502/fuzzing-lua + ligurio/lunapark; their ASAN/UBSAN/CBMC halves do not transfer to a JVM plugin, their oracle design does. Fuzzing engines (Jazzer/JQF) and `lua.bnf`-driven generation deferred — they produce inputs, worthless until a judge exists |
| BUG-409 | The parse oracle ignores annotator-level syntax errors | planned | M | — | MAINT-35 | ✓ Found by **MAINT-35 DR-01** (2026-08-05) and **re-framed 2026-08-06 after probing**. Recorded as "a bare name parses as a statement, so non-Lua text reports no syntax error", with a fix direction of narrowing `exprStatement` in `lua.bnf`. **The premise is false.** Measured: `from __future__ import braces` yields 0 `PsiErrorElement`s but **4 ERROR highlights** — `LuaStandaloneExpressionAnnotator` (`plugin.xml:179`) flags every non-call expression statement, which is the ordinary IntelliJ split of permissive parse + semantic diagnosis. The defect is the oracle's `lunarAccepts = (parseErrors == 0)` (`CorpusSweep.kt:47`, `LuaTortureCorpusTest.kt:116`), which cannot see annotator errors and so invents disagreements. The old fix direction would have made a working annotator dead code, degraded error recovery on every half-typed line, and needed a regeneration — to correct a number in a test harness. Plan: share one rule between annotator and sweep, teach the oracle to count it, then itemize and gate whatever false accepts survive. Full plan in `docs/features/bug-fixes/409-oracle-ignores-annotator-syntax-errors/` |
| BUG-410 | `RockspecSourcePathProviderTest` prewarm-scheduling test is flaky | todo | M | — | — | ✓ Observed 2026-08-05 during the MAINT-35 Phase 3 gate. `testConcurrentReadLockCallsScheduleSinglePrewarm` (`RockspecSourcePathProviderTest.kt:127`) failed with `expected:<1> but was:<0>` — no prewarm job scheduled — then **passed three consecutive re-runs with no code change**, and the full suite passed on the next run (2366/0). Nothing in that commit touches `rocks/`. The VM had just recovered from a network blip, so load/IO timing is the likely trigger: the assertion counts jobs scheduled within a window rather than waiting for one. Recorded rather than left as folklore — a suite that fails once in a while trains people to re-run instead of read, which is exactly how a real regression gets waved through. Fix direction: await the scheduled job with a latch/timeout instead of asserting on a count sampled at an arbitrary instant |
| BUG-411 | Vertical tab and form feed are not treated as whitespace | todo | C | — | — | ✓ Found 2026-08-05 by **MAINT-35-06's torture corpus on its first run** — the single disagreement across 1 696 fuzzer inputs, whose minimized witness is one `0x0B` byte. `lua.flex:36-38` enumerates whitespace as `[ \r\n\t]`, omitting `\v` and `\f`; PUC dispatches on `isspace()`, whose C-locale set is exactly that plus those two. Verified accepted by all five pinned oracles (5.1.5 → 5.5.1). Fix is two characters in `lua.flex` plus a regeneration — deliberately **not** `[:space:]`, which is Unicode-aware and would accept NEL/NBSP that luac rejects. 82 of the 1 696 inputs carry one of these bytes. Low: no hand-written Lua contains them |
| BUG-415 | Corpus baselines went stale through a type-engine change, and a 3× jump was never evaluated | todo | M | — | — | ✓ Found 2026-08-06 re-recording for BUG-409. **Two defects, one cause.** (1) BUG-397 (`f08d7ca3`) changed the type engine and landed without the corpus baselines being re-recorded — the sweep is `-PwithCorpus`, excluded from the routine suite and absent from CI, so the ratchet that exists to catch exactly this was structurally unable to run. (2) The movement was then never evaluated: zerobrane's `LuaTypeAssignability` **846 → 2594 (+207%)** while `LuaUndeclaredVariable` **1812 → 791 (−56%)** — consistent with `wx.*` globals becoming typed rather than undeclared, which is what BUG-397 intended, but *consistent with* is not *correct*. The alternative reading is 1748 new false positives, and re-recording cannot tell the difference — it accepts whatever it sees as the new floor. luacheck/luarocks/penlight moved too, by 6–126. Also unexplained: two sweeps of identical code gave 2549 and 2594, and it is **not** the BUG-390 flake (no `highlightFailures` key). BUG-409 had to re-record to gate at all, so the committed numbers are an unvalidated snapshot. Fix: sample ~20 zerobrane warnings and classify them before trusting the number; give the corpus gate a trigger that is not "whenever someone remembers" |
| BUG-416 | `nil | T` is reported as not assignable to `T`, pervasively | **done** | M | — | — | ✓ Found 2026-08-06 sampling for BUG-415. `LuaTypeAssignability` errors whenever a possibly-nil value reaches a table slot — idiomatic Lua, since an absent field read *is* `nil`. Measured on zerobrane (72 files): **2491 warnings, 46 shapes, and the top three are one defect** — `nil value is not assignable to { ... }` ×959, `nil | { ... } is not assignable to { ... }` ×421, `{ ... } | nil is not assignable to { ... }` ×421 = **1801 (72%)**. ZeroBrane is a shipped IDE; 1801 real type errors in 72 files is not a credible reading. **Caused by BUG-397** — before it free globals were untyped and the comparison never happened — but filed separately because that change is correct in its own terms and the fix belongs to the assignability rule. Direction: treat a `nil` arm as *gradual* at an assignment site, exactly as BUG-397's Phase 1 already treats an `any` arm. Investigate the 959 bare-`nil` cases first: a value inferred as exactly `nil` rather than a union suggests a dropped non-nil arm, a distinct inference defect. Unverified aside: 176 `Too few arguments` warnings — BUG-397's no-arity-check guarantee covers *stub* calls only, so these may be legitimate |
| BUG-417 | Undeclared-variable results depend on whether the type inspection ran | **done** | M | — | BUG-415 | ✓ Found 2026-08-06 implementing BUG-416, whose corpus measure would not stabilise. **Measured on zerobrane (72 files): undeclared inspection alone reports 1 954 warnings; with `LuaTypeAssignabilityInspection` in the same pass, 843 (main) or 1 563 (BUG-416 branch).** With assignability off, the two trees agree per-file **exactly** — so the fix changes nothing about resolution, and the coupling is pre-existing. File-scoped and content-dependent: `gui.lua` reports 0 undeclared in one regime, 116 in the other, 265 standalone. User-visible: whether `wx` is flagged depends on inspection scheduling. **Fixed 2026-08-06 — the hypothesis was wrong**: not ResolveCache but **file-wide error anchors** — `fromLuaType` synthetics anchor at `graph.firstNodeElement()` (the file), a failed synthetic check produced ERROR highlights spanning `0–43946`, and the platform hides lower-severity infos under an ERROR range (filetree: 5 file-wide errors buried all 126 warnings; instrumentation showed the visitor registered them all). Fix: re-anchor to the value element, drop when both sides are synthetic, `addError` refuses file-wide anywhere. Verified: 70/72 files exact parity, 1 947 vs 1 954 sweep-wide; residual 6 = narrow-anchor severity precedence, platform-by-design. Follow-up flagged, not taken: assignability's ERROR severity is a policy choice deserving its own decision. Original hypothesis for the record: snapshot computation (BUG-397 `resolveGlobal` seeding) resolves references mid-pass and `ResolveCache` serves those results to `LuaUndeclaredNames`. Ruled out empirically: highlight caps, solve-loop cutoffs, errors-as-progress, union propagation. **Every baselined `LuaUndeclaredVariable` count (1 812/791/829/1 645) is a distorted sample of ~1 954**, which also explains BUG-415's drift and part of BUG-397's apparent improvement |
| BUG-418 | Inspection counts drift ±3 across identical runs, against a ±0 gate | **done** | M | BUG-417 *(done — exposed this by making counts meaningful)* | — | ✓ Filed 2026-08-06. Same tree, three runs: zerobrane undeclared 1945/1947/1948; drift present in every configuration measured and **survives the BUG-417 fix**, so it is neither the contamination regime nor BUG-390's gated non-determinism (highlightFailures=0 throughout). Matters now because the instrument is finally honest and the recorded baseline is the LOW sample of its own distribution — an unlucky sweep fails as a phantom regression, which trains re-recording instead of reading (BUG-410's lesson one layer down). Candidates only, not conclusions: the ±6 severity-precedence boundary with run-varying emission order; daemon/fixed-point iteration order; JVM state. **Closed same day by localization: there is no drift.** Two identical probe runs: empty per-file diff; two full gate-shaped runs: identical across every member and inspection, both exactly equal to the recorded baseline (1945/997). The spread was three different invocation shapes, each byte-deterministic; cross-member JVM state shifts counts deterministically, and record/gate share a shape, so the ratchet always compares like with like. Real hazard documented in `LuaCorpusSweepTest` KDoc: a MEMBER-ALONE run reads ±2 off a full-run baseline by design — diagnostic usage, not the gate. Tolerance rejected: a ratchet leak for a nonexistent problem. Original fix preference for the record: LOCALIZE via per-file diff of two same-tree runs first; a declared tolerance is last resort and re-record-on-flake is not acceptable |
| BUG-407 | Corpus manifest was positional TSV, hand-parsed by two drifting readers | **done** | S | — | — | ✓ Found 2026-08-05 asking why MAINT-35 was using TSV; **fixed the same day** by migrating the format. `fetch-corpus.sh:50` read rows with `while IFS=$'\t' read -r name url commit roots prune lualevel`. **Tab is IFS *whitespace*, so bash collapses consecutive tabs** — every column after an empty field shifted left. Measured on the live manifest: for **luacheck** and **penlight** (the two rows with an empty `prune`) the shell bound `prune=LUA51`, and `prune` feeds `rm -rf "${dest:?}/$victim"` — so every fetch ran `rm -rf test/corpus/<name>/LUA51`, harmless only because that path does not exist. `CorpusManifest.kt` split the same bytes *without* collapsing, so the two parsers disagreed about one file: shell bound 6 positional fields, Kotlin read 7. **Known since MAINT-33** — `docs/features/maint/33-corpus-sweep/implementation-plan.md:69` carried an unchecked task describing this exact `rm -rf`, never done. **Fix: `corpus.tsv` → `corpus.json`, `fetch-corpus.sh` → `fetch-corpus.py`.** Not `awk -F'\t'`: the root cause is *two hand-rolled parsers*, and awk repairs one while leaving the other. JSON is read by a **library on both sides** — stdlib `json` in the fetcher (Python is already required by `scripts/*.py` and CI) and Gson in Kotlin (already imported by 8 production files, platform-provided) — so no schema knowledge is hand-written anywhere and no new dependency is added. `.properties` was considered and rejected: `grep`/`cut` on the shell side would have been hand-parsing again. Regression test: `emptyOptionalDoesNotShiftOtherFields`. Gates: corpus ratchet green on all four members, suite 2342/0 |
| BUG-408 | A separator in a file path silently dropped its docs from Search Everywhere | **done** | C | BUG-407 *(done)* | — | ✓ Found 2026-08-05 sweeping for other TSV-shaped formats after BUG-407; **fixed the same day**. `LuaDescriptionIndex` packed values as `"$ownerName\t$fileUrl\t$offset"`, joined with `|`, and `LuaDocSearchEverywhereContributor` hand-split them back. `ownerName` **was** sanitised (`replace(Regex("[\t|\n\r]"), " ")`) — `fileUrl` was interpolated raw, and a tab, `|`, newline or CR is legal in a POSIX filename (verified: `we\tird.lua` creates fine). Such a file produced a record of the wrong arity, which the reader dropped via `if (parts.size != 3) continue` — the file's documentation vanished from Search Everywhere with **no error anywhere**. Same class as BUG-407 (delimiter occurring in the payload), much smaller instance: it degrades by skipping rather than by `rm -rf`, and needs a pathological filename. Fixed by **owning the format**: a `DescriptionRecord` data class holds encode/join/parseAll, and writer, reader and tests all go through it — nobody hand-splits. Separators are **percent-escaped, not replaced**, because a URL must round-trip or the file cannot be reopened; `%` is escaped first and unescaped last so the mapping is a bijection (otherwise a literal `%09` in a path would decode to a tab). `getVersion()` 2→3 to rebuild stale entries. **Mutation-proved**: reverting the escape fails 4 of the 6 new tests, and the 2 that still pass are the ones that do not exercise it. Suite 2348/0 |
| MAINT-33 | Corpus sweep — regression ratchet over pinned real-world Lua projects | **done** | S | — | — | ✓ 2026-08-03 → `docs/features/maint/33-corpus-sweep/` (all 6 phases). Pinned checkouts (luacheck v1.2.0, luarocks v3.12.2, ZeroBrane 2.01) fetched out-of-repo to `test/corpus/`, swept headlessly for parse errors / unresolved `require`s / inspection hits / per-symbol breakdown / ballast, gated against committed baselines. Opt-in via `-PwithCorpus`; builder-only, never CI. **Found BUG-389, BUG-390, BUG-391, BUG-392 and BUG-393** — four fixed. Verification checklist run 2026-08-03: **12 pass, 0 fail** — Scenario 4.1 failed first time and the code was fixed (the ballast `claimed` flag was all-or-nothing, so one extensionless `rockspec` file hid 53 claimed ones). KOReader admitted, measured and parked: fits on disk (12 MB) but sweeps in 10.2 min, taking the corpus past its 10-minute ceiling. Complements the manual `vnc-multi-project-smoke-tests.md` |
| BUG-392 | Parser rejects valid Lua in luarocks' `cmd.lua` | **done** | **M** | — | — | ✓ Found by the MAINT-33 corpus → `docs/features/bug-fixes/392-parse-error-on-valid-luarocks-cmd/`. A long string opening on a **blank line** (`[[` + 2+ newlines) lexed as three tokens, not one: `lua.flex`'s `XLONGSTRING_BEGIN` emits one `NL_BEFORE_LONGSTRING` per newline without leaving the state, and `LongStringMergingLexerAdapter` consumed only the first (`if`, not `while`), leaking the body as tokens `lua.bnf` has no rule for. Both symptoms shared this root cause. luarocks `parseErrors` 1→0 and `highlightFailures` 1→0; luacheck `LuaTypeAssignability` 462→449. **Not** a pin/SYNTAX-18 defect — the error position was a rollback artifact of `funcBody`'s unpinned section |
| BUG-393 | Valid LDoc annotations reported as parse errors | **done** | C | — | MAINT-33 Penlight corpus row | ✓ Found by the MAINT-33 KOReader sweep, **fixed 2026-08-04** → `docs/features/bug-fixes/393-ldoc-annotation-parse-errors/`. **Two of the three reported constructs were real.** `description` refused `CODE` although `TAG_PARAM` lexes backtick spans (`luacats.flex:190`), and `@param[opt=false]` hit `paramTag`'s demand for a NAME or `...`. Fixed in `luacats.bnf`: `description` accepts `CODE`, and a new `ldocParamTag ::= '@param' description` sits after `paramTag` in `anyTag` so unmodelled shapes degrade to prose. Deliberately a **sibling element type**, not an alternative inside `paramTag` — that shape was tried first and makes `LuaCatsParamTag.getArgType()` `@Nullable`, breaking eight call sites in the type engine, stub builders and doc renderer. The third construct (`@func`) **did not reproduce**: it is absent from the `TAG` regex, so it already lexes as prose. Parser/lexer regenerated headlessly; `src/main/gen` committed. Suite 2341/0 |
| BUG-406 | LDoc's untyped `@param` is misread as a LuaCATS typed `@param` | **done** | C | BUG-393 *(done)* | — | ✓ Found 2026-08-05. LDoc writes `@param <name> <english description>` with **no type slot**; LuaCATS writes `@param <name> <type> <description>`, so the first word of the prose becomes the type — measured: `--- @param array Lua table of values` yields `argType='Lua'`, `@param e a value` yields `'a'`. **Priority cut M → C on 2026-08-06 after probing the two claims that justified the M; both are false.** (1) The type engine is unaffected — `LuaTypeGraphBridge:122` bails on an unresolvable name, and the probe reports `errors in graph = 0` with both params still inferred from usage. It is *not* why Penlight baselines `LuaTypeAssignability=570`. (2) Penlight is unaffected despite its 490 `@param` lines: it writes a `---` summary then `--` continuations, and `LuaLexer` only emits a LUACATS_COMMENT when **every** line starts with `---`, so those tags produce no `paramTag` at all. Real damage is the doc surfaces — `LuaParameterInfoHandler:140` and `LuaCatsDocumentationRenderer` show `array: Lua`. Fix at the display layer (render a declared type only when it resolves), not in the grammar: no parse-time heuristic separates `value` from a `@class Value`, and a per-file dialect signal misses 15 of Penlight's 26 bare-`@param` files. Full grounding in `docs/features/bug-fixes/406-ldoc-untyped-param-misread/` |
| BUG-391 | Globals assigned in one project file reported undeclared in another | **done** | **M** | — | — | ✓ Found by the MAINT-33 per-symbol breakdown → `docs/features/bug-fixes/391-cross-file-global-assignment-not-resolved/`. On ZeroBrane 2.01, 391 of 1009 `LuaUndeclaredVariable` warnings are uses of `ID` (`src/editor/ids.lua:205`) and `ide` (`src/main.lua:64`) — both plain top-level global assignments in indexed files. `level="ERROR"`, `enabledByDefault`, so it is a wall of false errors in any project sharing state through globals |
| BUG-390 | Type-graph cycle guard defeated by lazy nodes (StackOverflowError) | **done** | **M** | — | MAINT-33 Phase 3 inspection baselines | ✓ Found by the MAINT-33 corpus sweep → `docs/features/bug-fixes/390-type-graph-cycle-guard-defeated-by-lazy-nodes/`. `VariableElement.resolveWrite` threads its `visited` set through the `is VariableElement` branch (`LuaTypeNodes.kt:102`) but drops it at `:103`, so every `LazyValueElement` hop restarts with an empty set and a `variable → lazy subscript → same variable` cycle overflows the stack. **Endemic, not an edge case: 42/132 luacheck files and 59/159 luarocks files.** `resolveRead` (`:117`) has the same shape |
| BUG-389 | `require "mod"` contributes no reference | **done** | S | — | — | ✓ Found by the MAINT-33 corpus sweep → `docs/features/bug-fixes/389-string-call-require-no-reference/`. `args ::= … \| STRING` means the paren-less form is a bare STRING, not a `LuaTerminalExpr`, so `LuaRequireReferenceContributor` never matches it — no Go to Definition. The indexer *does* handle it (`LuaFileBindingsIndex.kt:326`), so completion and navigation disagree. luacheck: 3 references recognised across ~155 call sites |
## Wave 21 — Definition libraries & resolution correctness  *(shipped as v0.21.0)*

> **Versioning convention: wave N ships as `v0.N.x`.** This wave holds the work released in
> **v0.21.0** — TARGET-08's on-demand definition libraries and the completion/resolution defects
> that had to be fixed for them to work — plus the follow-ons that stream is still carrying.
> It was originally filed under Wave 20 and moved here on 2026-08-04 to restore the convention;
> the v0.21.0 tag and its published release are unchanged. Wave 20 keeps what actually shipped as
> v0.20.x (the MAINT-33 corpus sweep and its four fixes) plus the consolidated loose backlog.

| ID | Title | Status | Prio | Depends on | Unblocks | Parallel |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| TARGET-07 | Lua 5.5 standard-library stubs | planned | C | SYNTAX-09 *(Lua 5.5, done)* | — | ✓ planned 2026-07-16 → `docs/features/target/07-lua-5.5-stdlib-stubs/`. Additive resource-only: `runtime/standard/lua-5.5/` = copy of 5.4 + `table.create` + `_VERSION` bump; resolution already wired (`PlatformVersionRegistry`/`Target`/`RuntimeLibraryProvider` map 5.5→`lua-5.5`), **zero production Kotlin**. Note: roadmap brief was imprecise — `table.move` is already 5.3+, `global` is a keyword (SYNTAX-09), not a stub file |
| TARGET-08 | On-demand LuaLS / LuaCATS definition libraries | **done** | C | TARGET-04 *(Library Root Resolution, done)*; LuaCATS parsing *(done)* | — | ✓ **done 2026-08-04** — all 8 requirements Full; TC 6 (member completion through a registered root) verified live in GoLand. Delivering it took four fixes outside this feature's own code — BUG-395, BUG-398, BUG-399 and BUG-394 — every one of them invisible to an ordinary light fixture, whose project sits entirely inside `projectScope`. **Closed with one acceptance box unticked**: the `love2d` real-flow DoD was never run; the same flow is proven for busted/luassert, so what love2d adds is scale against the untouched `MAX_CANDIDATES = 500` cap. Planned 2026-07-16 → `docs/features/target/08-on-demand-definition-libraries/` (5 phases): bundled curated JSON catalog → per-project enable list in `.idea/lunar.xml` → leaf `LuaArtifactDownloader`/`LuaArchiveExtractor` fetch (not the heavy provision pipeline) → `LuaDefinitionLibraryProvider : AdditionalLibraryRootsProvider` (models `LuaRocksLibraryProvider`) → settings UI. Bundle-nothing licensing (URLs+hashes only). Kept under TARGET; graduate to a `DEFS` epic only if a browse/install catalog UI is greenlit. Original scoping note: support consuming community **LuaLS/LuaCATS definition libraries** (the [LuaCATS](https://github.com/LuaCATS) org repos + LuaLS addon ecosystem — love2d, busted, luassert, openresty, …) for **third-party** libraries a project uses, **fetched/enabled on demand** rather than bundled. Distinct from the first-party platform stdlib stubs under `runtime/` (TARGET-07 / TARGET-04): reuse `PlatformLibraryProvider`'s library-root injection to register a fetched definition tree, parse its `@meta` LuaCATS annotations (existing support), and cache per project. Fetch mechanic overlaps TOOLING provisioning (cf. TOOLING-04 already downloads the `lua-language-server` binary). Community defs carry their own licenses (mostly MIT) — attribute any that get bundled/cached; a purely on-demand, user-selected fetch is package-manager-like. **Epic placement is a judgment call** — filed under TARGET for the library-resolution reuse; could graduate to its own epic (or a TOOLING id) if it grows |
| BUG-394 | Bare-identifier global completion is scoped to project files, so library symbols never appear | **done** | S | — | — | ✓ Found by TARGET-00-DR-03 (2026-08-03), **fixed and verified live 2026-08-04**. `GlobalSymbolRankingService` searched `GlobalSearchScope.projectScope` at `:110` and `:180`, and project scope excludes library files by construction — so no `SyntheticLibrary` symbol ever completed in any caret position, and this was never only about definition libraries: `print` (`builtin.lua:114`) went through the same path, so the **entire Lua standard library** was missing from the caret a user is most likely to use. Now `allScope`. The roadmap's own warning — *measure ranking/perf impact rather than flipping it blind* — earned its keep; widening alone broke two things the tests caught. **(a)** Library symbols were handed to `LuaAutoImportInsertHandler`, so accepting `print` wrote `require("…/lunar-0.18.0.jar!/runtime/standard/lua-5.4/builtin")` into the file — `print` is ambient and a `@meta` file is not a requirable module, so library symbols now get no import handler. **(b)** `goto <caret>` began listing the whole stdlib; that caret is a *label* position served by `LuaLabelReference`'s variants, and the cross-file provider is now excluded from it as it already is after `.`/`:`. Ranking: library symbols score below the lowest weight any project file can reach, and are excluded from the recency bonus (a library extracted an hour ago is not "a file you were working in"). Verified live: `pri` → `print` (tagged `builtin.lua`, no auto-import tail) inserting `local x = print`, and `descr` → busted's `describe` |
| BUG-396 | Definition-libraries settings page does slow VFS I/O on the EDT | **done** | S | — | — | ✓ Found **and fixed** by VNC verification of TARGET-08 (2026-08-04) — **not catchable by any unit test**, the platform assertion only fires in a running IDE. `LuaDefinitionLibrariesConfigurable.createComponent` → `LuaDefinitionLibraryEnabler.rows` → `LuaDefinitionLibraryFetcher.cachedRoot` logs SEVERE *Slow operations are prohibited on EDT*: `VfsUtil.findFile` is itself a prohibited slow op, so replacing `listDirectoryEntries()` with a VFS lookup moved the call without fixing the class of problem. Logged rather than thrown, so the page renders and the feature works. **Fixed** by removing the EDT disk access rather than suppressing: every `SlowOperations.allowSlowOperations` overload is deprecated and only `knownIssue(ytIssueId)` remains, which is a debt marker, not a fix. `rows()` is now catalog-only (no I/O), `fetchedIds()` is explicitly off-EDT, `apply()` computes `missingEntries` inside its background task, and the settings page fills the status column from a pooled thread. Re-verified live: fresh log shows **0** occurrences (was 3) and the column still resolves to Fetched. **Still open — `LuaRocksLibraryProvider.installedRoots` uses the same `VfsUtil.findFile(…, false)` shape** and may be a pre-existing instance; it produced no SEVERE in this run, so it was left alone rather than changed on a hunch |
| BUG-395 | Member completion after `.` is file-local, so library members never appear | **done** | S | — | TARGET-08-04 (TC 6) | ✓ Found reviewing TARGET-08 Phase 4 (2026-08-03), **fixed 2026-08-04**. The post-dot caret is served only by `LuaCompletionContributor`'s member provider, which reads `LuaTypesSnapshot.forFile(...).getValueType(...).getMembers()` and never consults `GlobalSymbolRankingService` — so widening BUG-394's search scope would not have fixed it. Two defects, both fixed: **(1)** a declaring file did not publish its globals — `X = {}` and `function X.y()` landed on *unrelated* nodes because a forward pass only learns a global exists at its first write, and `VariableElement.resolveRead` took the **first** demand, so a stub declaring ten `function table.<name>` described `table` by exactly one member. `LuaTypesVisitor` now pre-declares file-scope globals into the root scope and publishes them via `LuaTypes.getGlobalType(name)`; table demands merge instead of first-wins. **(2)** nothing consumed them cross-file — new `LuaTypeManager.resolveGlobal(name, ctx)` looks the name up in `LuaGlobalAssignmentIndex` under **`allScope`** (the point: library files are outside `projectScope`) and the member provider falls back to it when the receiver's own type is `Undefined`. `table.`/`table:` and a `Lib = {}` in any project file now complete; `assert = require("luassert")` resolves through the require chain. Deliberately **not** wired into `visitNameRef` — see BUG-397 |
| BUG-397 | Free globals are typed for completion only, not for the inference engine | **done** | S | BUG-395 *(done)* | — | ✓ **Fixed 2026-08-05** as the two decisions below, landed as three separately-gated phases (each full-suite green): **(1)** `LuaTypeAlgebra` — `any \| <structural>` keeps its structural arms; a union keeping an `Any` arm is *gradual* (never an assignability error, only matching arms propagate constraints). **(2)** Declared types are authoritative — `visitIndexExpr` types a free-global member from `resolveGlobal` as a plain ValueNode (typing, not checking: `visitFuncCall` flows a declaration-typed callee's declared return into the call results and raises **no call demand**, preserving the "stub calls are not arity/param-checked" contract that both reverts broke). **(3)** The `visitNameRef` fallback itself, which then displaces nothing. All three historical regression shapes are characterized in `FreeGlobalMemberTypingTest` (the pcall union asserted **without** any member usage in the consuming file — the old TC-PCALL-1 pattern reflected the use constraint back and could not see a collapsed union). **Adversarially reviewed 2026-08-05 (verdict MERGE-WITH-FIXES, fixes applied):** the blocker — chained access `A.b.c` on a free-global receiver fell through to the bare-receiver-anchored graph path and resolved as `A.c` (0→4 false positives + cross-chain pollution through shared seed member nodes) — is fixed: chains resolve hop-by-hop through declared types, and an undeclared link leaves the rest node-less; pinned by three new chain tests. **Known follow-ups from the review, deliberately not in this change:** (a) `LuaGlobalAssignmentIndex` only indexes assignment targets, so bare `function print(...) end` globals (most of the base library's free functions) still don't resolve — extending the indexer + version bump delivers them; (b) argument-type mismatches anchor/multiply badly (pre-existing `injectParamAnnotations` anchor scheme, newly reachable now that args have types); (c) a snapshot built during dumb mode caches seed-less free globals until the next PSI change, and cross-file global cycles cache their degraded first answer (`resolveGlobal`'s reentrancy break is uncached but its caller's result is not); (d) `VariableElement.resolveWrite` builds raw non-canonical unions that bypass `LuaTypeAlgebra.canonicalize` (latent, no demonstrated defect); (e) only return #1 of a declared function flows (`LuaGraphType.fromLuaType` models a single return). BUG-359 died in phase 2: the false positive was the seed-less `package.path` read hitting `visitBinOpExpr`'s `graph.nil` operand fallback (a Nil value meeting the concat's String use) — not the assignment LHS. `DuplicateNilAssignabilityTest` inverted to zero per the landing note; its duplicate-surfacer concern re-pinned on a genuine `nil .. s`. Original analysis follows. Carved out of BUG-395 (2026-08-04) after being tried and reverted with evidence. Routing `LuaTypeManager.resolveGlobal` through `LuaTypesVisitor.visitNameRef` types every free global for *all* consumers (hover, inlays, inspections) — and immediately regressed four suites, because the checker starts checking calls it previously skipped: `table.sort`'s comparator inferred `boolean \| any` (`ExpectedCallbackResolverTest`), `redis.register_function`'s table form grew two spurious argument errors, `redis.pcall`'s reply lost its `{ err: string }` arm, and a genuine nil-assignability error stopped being reported. The reverted patch is quoted in `LuaTypesVisitor.visitNameRef`'s KDoc. **Correction 2026-08-04: three suites, not four.** The fourth — "a genuine nil-assignability error stopped being reported" — was not genuine; it was **BUG-359's false positive disappearing**, i.e. an effect to want, not a regression to fix. **Now known to fix BUG-359 outright:** `package.path = "..."..package.path` flags because the LHS `package.path` types as `Undefined` while the RHS correctly types as `String`, and `resolveGlobal("package")` already returns `path` — the engine simply is not allowed to ask. **Landing note:** `DuplicateNilAssignabilityTest` asserts that false positive is surfaced *exactly once* (its concern is duplicate reporting), so it pins the bug as expected behaviour and must be inverted to zero as part of this fix, or it will fail and look like a regression. **Attempted 2026-08-04 and reverted a second time — but the mechanism is now identified, so the next attempt starts from a cause rather than three symptoms.** Binding the receiver *displaces a better-informed member path*: with `redis` unbound, `visitIndexExpr` returns early at its `firstNode(...) ?: return` and `redis.pcall`'s type comes from the stub route, which reads `---@return any|{ err: string }` as a Layer-1 union with both arms intact. Bind `redis` and the member flows through a graph `Table` constraint instead, where `LuaTypeAlgebra.simplify` collapses `any | X` to `Any` the moment any arm is `Any` — so the `{ err: string }` arm and `reply.err` vanish. The same displacement explains the other two. **So the work is two decisions, not three bug-fixes:** which path owns member typing on a cross-file receiver, and whether `any | <structural>` should keep its structural arms. Both are engine changes with their own blast radius, which is why this is not a drive-by. The reverted patch and this analysis are in `LuaTypesVisitor.visitNameRef`'s KDoc. Landing note unchanged: `DuplicateNilAssignabilityTest` must be inverted to zero |
| BUG-398 | A required module's exported type loses its `@class` parent and leaks the file's locals | **done** | C | — | TARGET-08-04 (TC 6) | ✓ Found while testing BUG-395, **fixed 2026-08-04**. Given luassert's real shape — `---@class luassert : luassert.internal` on a `local`, `return luassert` — a consumer's `assert.` (via `assert = require("luassert")`) completed `[unregister, internal, luassert]`; it now completes `[are, True, unregister]`. **Three distinct defects.** **(1)** `collectMethodMembers` and `LuaImplicitFields` matched members on the *class* name, but LuaCATS names a class apart from the local carrying it (`---@class luassert.internal` on `local internal = {}`) and the members are written against the **variable** — so the parent class materialized with **no members at all**, and everything inheriting from it inherited nothing. Both now scan the class name project-wide plus each declaring local's name **confined to its own file**. **(2)** `LuaTypeManagerImpl.getModuleType` ran live analysis only when a file had *no stub*; a stub whose `exportedTypeString` was empty returned null and the module lost its type outright. Since stubbed-vs-AST-backed depends on nothing but what loaded the AST first, the same `require` resolved differently from one caret to the next — this is why the symptom looked intermittent. Live analysis is now a real fallback. **(3)** `LuaCrossFileCompletionProvider` was registered on any IDENTIFIER, so it also fired *after* a dot and answered a member caret with project-wide globals and `---@class`-carrying locals — the `internal`/`luassert` leak. Now excluded after `.`/`:` |
| TARGET-09 | Addon auto-detection (turn TARGET-08 from capability into fix) | **planned** | S | TARGET-08 *(done)* | — | ✓ **Planned 2026-08-04** → `docs/features/target/09-addon-auto-detection/` (5 phases): catalog gains `detectionPatterns` curated from upstream `words` → a Lua-pattern→regex translator → a pure per-file detector → a Lua-file editor banner modelled on `LuaToolEditorNotificationProvider`. **Resolved product decision: suggest, never fetch** — a silent enable would trigger an unrequested download, so the banner offers Enable / Not now / Never-for-this-project and routes Enable through the existing `LuaDefinitionLibraryEnabler.apply`. Detection patterns are **bundled, not read from the fetched tree**: detection has to work before the fetch it exists to suggest. Known coverage gap recorded rather than papered over — upstream `busted` declares no `words`, so a project using `describe`/`it` without requiring luassert is not detected, which is the strongest argument for also taking the cheap `LuaUndeclaredVariable` fix. Originally flagged 2026-08-03 as **needing investigation** → detail in `docs/features/target/08-on-demand-definition-libraries/risks-and-gaps.md` (Future Work). TARGET-08 ships enablement only — nothing improves until a user ticks a library. Corpus evidence: **1507 of 1569 (96%)** `LuaUndeclaredVariable` hits across luarocks + luacheck are busted globals, at `level="ERROR"` and `enabledByDefault`. **The detection signal no longer needs inventing (found 2026-08-04):** every catalog entry is a **LuaLS addon** shipping a `config.json` manifest that declares its own trigger — luassert `"words": ["require[%s%(\"']+luassert[%)\"']"]`, love2d `"words": ["love%.%w+"]`. Detection is "match each entry's `words` against project sources", not a per-framework heuristic. What it *does* when it fires (silent enable triggers a network fetch; suggest/quick-fix may be better) is still open. Same manifest also carries three things our catalog hand-derives and can drift from — `requires` (busted declares its luassert dependency as `${3rd}/luassert/library`; DR-01 re-derived it by hand), `displayName` (`"name": "LÖVE"`), and **runtime version** (love2d declares `LuaJIT`, which we ignore despite having a first-class target concept). **Cheap prerequisite:** `rootPrefix` currently extracts `<repo>/library` only, so `config.json` never reaches disk — retain it first. **Budget the pattern translation:** `words` are Lua patterns, not regex. **Price the cheaper alternative first** — teaching `LuaUndeclaredVariable` about test-framework globals directly removes the same 96% with no catalog/fetch/UI. Note the corpus likely cannot measure either fix without a pre-seeded fixture |
| TARGET-10 | `wx`/`wxstc`/`wxaui` definition libraries — investigate, then catalog | todo | C | — | MAINT-37 *(zerobrane member scope)* | ✓ Filed 2026-08-06. The catalog today is busted/luassert/love2d/openresty — **no wx**, and whether usable upstream LuaCATS/LuaLS wx definitions exist is a fact to VERIFY, not assume (this session's recurring lesson: three of three bug reports carried refuted premises). Investigation first: check LuaCATS org + LLS-Addons for wx/wxwidgets/wxlua coverage and licensing; if none exists, adopting or authoring one is its own cost/benefit call, priced before committed. If found: one catalog entry (pin + sha256 + rootPrefix + license), then zerobrane joins MAINT-37's addon-enabled members. Evidence of value: zerobrane's uncontaminated undeclared count is ~1954, dominated by `wx` (1263+) — the single largest missing-definitions bucket in the corpus |
| BUG-399 | A `---@class` declared in a library file cannot materialize (projectScope-bound) | **done** | **M** | — | TARGET-08-04 (TC 6) | ✓ Found re-running TC 6 live over VNC, **fixed and re-verified live the same day** (2026-08-04). `LuaTypeManagerImpl.doResolveType` and `collectMethodMembers` searched `GlobalSearchScope.projectScope`, which excludes library files by construction — so a `---@class` in a bundled stdlib stub, definition library or LuaRocks tree did not exist as far as the type manager was concerned, and every member of it went missing. Both now use **`allScope`**. **Only observable with a real registered library root**: everything in a light fixture's own project is in project scope, which is exactly why the BUG-395/BUG-398 unit tests passed while the live IDE showed nothing — guarded now by `LuaLibraryModuleResolutionTest`, built on the DR-03 harness. Two live readings from the first VNC pass were **wrong** and are corrected here: `require("luassert")` *does* resolve into a library root (the harness proves it; the red squiggle and the Ctrl+B miss were misread — Ctrl+B also missed on a `require("main")` that resolves fine), and `resolveModuleCandidates` was never at fault. Distinct from BUG-394, which is `GlobalSymbolRankingService`'s own projectScope on the bare-identifier caret and is still open |
| BUG-400 | `---@type <stdlib class>` resolves to nothing — no library class is nominally resolvable | **done** | S | — | — | ✓ Found while diagnosing BUG-359 (2026-08-04). **Not part of BUG-359**, which the graph path answers and BUG-397 closes; this is the separate nominal path, `LuaTypeManagerImpl.doResolveType`. Measured: `resolveType` returns **null for `package`, `io`, `os`, `debug`, `coroutine` and `utf8`** — every stdlib library class — and `---@type package` + `p.` completes **nothing**, while the graph path completes `[cpath, loaded, loadlib, path, preloaded, searchers, searchpath]` for the same receiver. **Cause:** `LuaClassNameIndex` is a stub index over `LuaLocalVarDecl`, and LuaCATS tags are not stubbed — they ride a host decl's stub. The stdlib stubs declare their classes on a bare **global assignment** (`---@class package` above `package = {}`), which is not a stubbed PSI type, so the tag is never hoisted and never indexed. `string`/`table` only *appear* to resolve because `resolveType` checks `LuaPrimitiveType.PRIMITIVES` first and those names collide with primitives — it is not finding the class. **Fix direction:** the file-based `LuaCatsTypeNameIndex` already exists for exactly this (it reads `LuaCatsClassTag.argType` directly, so a bare `---@class` is indexed) and is used by Goto Class and documentation, but **not** by the type manager. Fall back to it in `doResolveType` when the stub index misses. **Fixed 2026-08-04** exactly that way: `doResolveType` falls back to `materializeUnhostedClass`, which finds the tag through `LuaCatsTypeNameIndex`, reads its `@field`s and `: Parent` list off the enclosing comment, and reuses the same implicit-field and method collection a hosted class gets. `LuaImplicitFields.collect` now takes receiver names + files instead of `LuaLocalVarDecl`s, so an un-hosted class can use it. All six stdlib classes resolve, `---@type package` completes `path`, and bare `---@class` inheritance works. Guarded by `LuaUnhostedClassResolutionTest`. **Also affects** anything reaching for a stdlib class nominally — hover/quick-doc, `LuaClassType.resolveMember`, parameter inlay hints, and the NAV-05/06 hierarchy — which were all broken for stdlib classes and are now at least reachable, though only completion is asserted |
| BUG-401 | `---@field name?` yields a member literally named `name?` | **done** | **M** | — | — | ✓ Found fixing BUG-400, **fixed 2026-08-04**. `LuaLocalVarStubElementType` built `luacatsFields` with no optional handling at all, so `---@field beta? number` was keyed **`beta?`** — a member no lookup can ever match. **Every optional field on a stubbed class was silently unreachable**: no completion, no resolution, no hover. Raised from C to M once located: the stub path is the one a *hosted* class takes, i.e. the normal case, not an edge. `materializeClass`'s AST branch had always stripped the marker, which is why nothing caught it. The stub now strips it and widens the type to `(T) \| nil`, and `LuaFileElementType.getStubVersion` is bumped 2→3 so stale stubs rebuild. Guarded by `LuaOptionalFieldTest`. **My first diagnosis put it in the AST branch and was wrong** — the fixture had a trailing `local`, which gave the tag a stub host and routed resolution through the very path being blamed |
| MAINT-34 | LuaCATS extraction unification (stub ↔ AST parity) | **planned** | M | — | BUG-402 | ✓ **Planned 2026-08-04** → `docs/features/maint/34-luacats-extraction-unification/` (Phase 0 de-risk, then 5 phases). Every LuaCATS tag that feeds the type engine is read **twice by two different pieces of code** — `*StubElementType.createStub` at index time and `LuaTypeManagerImpl` from live PSI — and which one runs depends only on whether the file's AST happens to be loaded. Measured drift: **BUG-401** (stub kept the `?` marker, AST stripped it), **BUG-402** (stub splits a parameterized parent on `,`, AST does not) and **BUG-400** (closing it required writing the extraction a *third* time). Extracts one `LuaCatsDeclarations` object both sides call, so the fork becomes a data-source choice, not two parsers; stub stores parents as a `List<String>` (stub version 3→4). Also lands the **stub↔AST parity harness**, because the drift is invisible to the ordinary test idiom — measured: `configureByText` exercises the **AST** branch, `addFileToProject` the **STUB** branch, so 2 of BUG-401's own 3 regression tests passed with the bug present |
| MAINT-37 | Corpus sweeps run with pinned definition libraries | todo | S | **BUG-417** *(inspection independence — must land first or the re-baseline is unattributable)*; TARGET-08 *(done)* | — | ✓ Filed 2026-08-06. The corpus measures the UNCONFIGURED experience: luacheck's `spec/` tree is busted code and its undeclared count is dominated by busted globals TARGET-09 already measured at 96% across luarocks+luacheck — definitions we ship the remedy for but never apply in measurement. Add `definitionLibraries` per member to `corpus.json` (**hard-pinned**, never TARGET-09 auto-detection — the ratchet needs deterministic inputs), fetch the addon archives pinned+sha256 into the out-of-repo `test/` tree with the same script/stamp/fail-fast-guard pattern as corpus/luac/torture (fourth instance), register through the TARGET-08 path so library registration itself gains a scale-level regression surface, re-baseline once. Also feeds the type engine large real APIs — the surface class that exposed BUG-416. Start with luacheck+busted+luassert (catalog has them today); zerobrane's `wx` waits on TARGET-10 |
| BUG-402 | A parameterized `@class` parent is split in half on the stub path | todo | S | — | — | ✓ Found 2026-08-04 probing the stub/AST divergence for MAINT-34, which owns the fix (MAINT-34-02). `---@class Kid : Base<string, number>` yields **2** supertypes `Base<string` and `number>` when the declaring file is stub-backed, and **1** correct `Base<string, number>` when it is AST-backed — so a class loses its inheritance depending on whether its file happens to be open. `LuaLocalVarStubElementType` stores `parentTypes.text` flattened and `LuaTypeManagerImpl.kt:212` re-splits it on `','`; both other paths walk `parentTypes.argTypeList`, which `luacats.bnf:92` already split correctly. Note the fragments' `toString()` reads back as the original string — the defect is only visible if you count the list |
| BUG-403 | Lunar hard-depends on the `glimmer/luacheck` fork without declaring or enforcing it | todo | S | — | — | ✓ Found re-running the TOOLING-08 VNC pass (2026-08-04); **fork side confirmed 2026-08-04** against `~/Documents/src/lua/tools/luacheck` (remote `gitea.internetisalie.net/glimmer/luacheck`). Our fork **does** define the stds Lunar emits: `src/luacheck/builtin_standards/` ships `redis5.lua`, `redis6.lua`, `redis7.lua`, `redis_scripts.lua`, `redis_functions.lua`, and `init.lua:271-277` composes `redis5 = lua51+redis5+redis_scripts`, `redis6 = redis5+redis6+redis_scripts`, `redis7 = redis6+redis7+redis_scripts` (plus an unused `redis7_functions`), registered as builtins by the `lua_defs` loop at `:279-281`. So all five Redis/Valkey entries in `PlatformVersionRegistry` map to stds that exist — **the implementation is internally consistent**. The defect is the *undeclared dependency*: probed against upstream luacheck 1.2.0, `redis5`/`redis6`/`redis7` are all `unknown std` (as is `lua55`), and an unknown std is a **Critical error** that aborts the run, so the inspection yields zero diagnostics rather than degrading. Lunar ships no `.luacheckrc` defining them (`LuacheckrcSchemaProvider` is only a JSON schema), does not detect the fork, and does not document the requirement. **TARGET-05's design already specified the mitigation** — `docs/features/target/05-luacheck-integration/design.md`: "If the luacheck invocation fails with an unrecognised std, fall back" — and it was never implemented; `LuaCheckCommandLine.kt:78-82` adds `--std` unconditionally. Decide the contract: detect-and-retry, bundle the std definitions, or declare the fork a hard requirement and surface it in health diagnostics |
| BUG-405 | OpenResty/NGX target emits no luacheck std although `ngx_lua` exists | todo | C | — | — | ✓ Found confirming BUG-403 (2026-08-04). `PlatformVersionRegistry.kt:39-41` sets `luacheckStd = null` for NGX (and Tarantool, Pandoc), and `docs/features/target/05-luacheck-integration/design.md` justifies it as "platforms where luacheck has no bundled standard library definition". **That rationale is false for NGX**: `ngx_lua` is a builtin std in *both* upstream luacheck 1.2.0 (probed: accepted) and our fork (`init.lua:270`, `ngx_lua = luajit + ngx`). So OpenResty projects silently get no `--std` when a correct one is available in every luacheck build. Unlike BUG-403 this needs no fork. Same doc also lists STANDARD 5.5 → `lua55`, which does not exist in either build — the code correctly emits `lua54`, so the design table is stale, not the code |
| BUG-404 | Un-pinning a platform target to *Auto* never reflows to the runtime | **done** | **M** | — | — | ✓ Found re-running the TOOLING-08 VNC pass (2026-08-04), **Scenario 1.3 FAIL**. After pinning Redis `7+` then setting *Platform target* back to *Auto (from runtime)* and clicking OK: `explicitTarget` is correctly dropped from `.idea/lunar.xml`, but `TargetState` stays `{platform=Redis, versionLabel=7+}`, `languageLevel` stays `Lua 5.1`, the disabled Version combo still displays `7+` instead of the runtime-derived `5.4`, and the External Libraries node stays *Lua 5.1* against a Lua 5.4.7 runtime. **Re-running Auto-Discover does not recover it** — it is stuck, not merely stale. Tell-tale: the same page's *Resolved Runtime* block reports *Language level: **Lua 5.4*** under the caption "Reflects applied settings", so the panel's computed value and the applied state disagree. User impact: switching a project off a pinned platform silently leaves the whole language level and stdlib pinned to the old target . **Fixed 2026-08-06.** Root cause: `LuaTargetSynchronizer.recompute()`'s `lastAppliedRuntimeId` guard memoises *"the runtime id has not changed"* and is used to conclude *"the applied target reflects the runtime"* — different claims. Pinning diverges the two with the runtime id untouched, so on un-pin the guard suppressed the recompute, permanently. Confirmed by clearing the guard and re-running the identical call, which recovers. `ensureSynchronized()` now clears it: the guard debounces the *event* path, and both production callers (startup, settings Auto branch) are explicit requests. **Why no test caught it:** all seven existing `LuaTargetSynchronizerTest` cases clear the guard via `prepareBaseline()`/`pinExplicitTarget()`, so the sticky path was untestable by construction |

---



---

## Wave 22 — AI integration  *(AI epic)*

> **Deprioritised on 2026-08-06 — ordering only.** AI work does not precede quality work, so the epic's front-matter `priority` is `low`. The **Prio** column below is unchanged and deliberately so: MoSCoW records whether a feature is *required for its epic*, which is a different axis from *what gets picked up next*. AI-01 is still a `Must` for the AI epic — there is no AI epic without the MCP server — it is simply not scheduled while MAINT and BUG items remain open.

| ID | Title | Status | Prio | Depends on | Unblocks | Parallel |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| AI-01 | MCP Server Integration | todo | M | `com.intellij.mcpServer` bundled plugin (optional dep) | AI-02, AI-03 | Serial: registration foundation |
| AI-02 | Semantic Context Toolset | todo | S | AI-01 *(lunar-mcp.xml infra)* | — | after 01 |
| AI-03 | Debugger Toolset | todo | C | AI-01; **MAINT-24** *(done; debugger hardening — formerly "MobDebug hardening, unscheduled MAINT"; scoped 2026-07-17)*; REDIS-02 *(soft, LDB binding)* | — | after 01 + hardening |
| AI-04 | LuaCATS Annotation Generator | todo | S | — *(type engine done)* | — | ✓ (engine-only, no MCP) |
