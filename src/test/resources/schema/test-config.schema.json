{
  "type": "object",
  "required": ["name"],
  "properties": {
    "name": { "type": "string" },
    "opts": {
      "type": "object",
      "description": "Optional settings table",
      "properties": {
        "level": { "type": "string", "enum": ["low", "high"] }
      }
    },
    "tags": { "type": "array", "items": { "type": "string" } }
  },
  "additionalProperties": false
}
